{ This is a simple TINY program }
read x; { Read input into variable x }
if x > 0 then
   { If x is greater than 0, execute the following }
   repeat
      x := x - 1;
      write x;
   until x = 0
else
   { If x is not greater than 0, output 0 }
   write 0
end
